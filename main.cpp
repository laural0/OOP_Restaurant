#include <iostream>
#include <sstream>
#include "Ingredient/Ingrediente.h"
#include "Preparate/Preparate.h"
#include "Comanda/Comanda.h"

//            listaIngrediente.emplace_back(lista[0].c_str(), stof(lista[1]), stof(lista[2]), stof(lista[3]), dePost); //se blocheaza aici

int main() {


    Comanda a(7);
    std::cin>>a;
    std::cout<<a;
//     std:: cout << a.get_sumaDePlata();



}

